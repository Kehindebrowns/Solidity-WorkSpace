// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

contract Lesson {
       address private owner;
       constructor(){
        owner = msg.sender;
       }
     function name() public pure returns (string memory ,uint uint) {
         return (name,age ,);
     }
      function named() public pure returns(uint x , bool b){
        return (1 , true);
        x = 1;
        b = true;
      }
      // create an array with memeory
      uint[] memeory a =  new  uint [](5);
      a[1] = 123;
}